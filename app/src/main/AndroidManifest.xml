<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tokonedimas">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.tokonedimas.SearchProductsActivity"></activity>
        <activity android:name="com.tokonedimas.SettinsActivity" />
        <activity android:name="com.tokonedimas.ProductDetailsActivity" />
        <activity android:name="com.tokonedimas.ConfirmFinalOrderActivity" />
        <activity android:name="com.tokonedimas.CartActivity" />
        <activity android:name="com.tokonedimas.Admin.AdminMaintainProductsActivity" />
        <activity
            android:name="com.tokonedimas.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.tokonedimas.ResetPasswordActivity" />
        <activity android:name="com.tokonedimas.Admin.AdminUserProductsActivity" />
        <activity android:name="com.tokonedimas.Admin.AdminNewOrdersActivity" />
        <activity android:name="com.tokonedimas.Admin.AdminCategoryActivity" />
        <activity android:name="com.tokonedimas.Admin.AdminAddNewProductActivity" />

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity android:name="com.tokonedimas.LoginActivity" />
        <activity android:name="com.tokonedimas.RegisterActivity" />
        <activity android:name="com.tokonedimas.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>